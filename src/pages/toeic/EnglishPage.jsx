import { useQuery } from "react-query";

const EnglishPage = () => {

    // const { data, isLoading, error } = useQuery(['books1'], async () => {
    //     console.log('call fetchBook 2')
    //     const response = await fetch('http://localhost:3000/books?_embed=units')
    //     return response.json();
    // })

    // if (isLoading) return 'Loading...';
    // if (error) return `An error occurred ${error.message}`;

    // console.log(data)

    return ( <>
    
    Welcome English Page Practice: TOEIC ETS 2020, 2021, 2022
    
    </> );
}
 
export default EnglishPage;